#import '../fragments/FictionEditPageQuery_fiction.graphql'

mutation EditFictionMutation($input: EditFictionInput!) {
    editFiction(input: $input) {
        fiction {
            ...FictionEditPageQuery_fiction
        }
    }
}