{% set current = app.request.attributes.get('_route') %}

{% set links = [
    { name: 'admin.navigation.dashboard', path: path('admin.index'), active: current == 'admin.index' },
    { name: 'admin.navigation.fictions', path: path('admin.fiction_new'), active: 'fiction' in current }
] %}

{% set rendered %}
    {% include 'partials/_language_selector.html.twig' %}
{% endset %}

<admin-sidebar sitename="{{ sitename }}" :links="{{  links|json_encode }}" :footer-content="{{ rendered|json_encode }}">

</admin-sidebar>